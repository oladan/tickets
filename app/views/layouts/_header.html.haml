= nav_bar fixed: :top, brand: "Tickets" do
  = menu_group do
    = menu_item "Home", root_path
    = menu_item "Departments", departments_path
    = menu_item "States", states_path
  - if current_owner
    = menu_group pull: :right do
      = menu_item "Log Out", destroy_owner_session_path, method: :delete
  - else
    = menu_group pull: :right do
      = menu_item "Sign Up", new_owner_registration_path
      = menu_item "Log In", new_owner_session_path
